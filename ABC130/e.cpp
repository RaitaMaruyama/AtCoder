#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main(){
  int N, M;
  cin >> N >> M;
  int  S[N+1];
  int  T[M+1];
  long long ans = 0;

  for(int i = 0; i < N; i++){
    cin >> S[i];
  }
  for(int i = 0; i < M; i++){
    cin >> T[i];
  }

  for(int i = 0; i < N; i++){
  }

  return 0;
}
